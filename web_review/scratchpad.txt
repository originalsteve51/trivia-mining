

Start the webviewer as follows:

./activate_run.sh dev_web.sh

    -- OR --
./activate_run.sh prod_web.sh

The host info in your choice is used to run the web server.   

On server svpserver5.ddns.net, Auto start after re-boot is done via an entry
in /etc/rc.local  